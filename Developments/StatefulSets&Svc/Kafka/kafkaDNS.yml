# A headless service to create DNS records
---
apiVersion: v1
kind: Service
metadata:
  name: broker
  namespace: disloman-local
spec:
  ports:
  - port: 9092
  # [podname].broker disloman-local.svc.cluster.local
  clusterIP: None
  selector:
    app: kafka